package tests;

import static org.junit.Assert.fail;

import org.junit.Test;

class EditDocumentTest {
	/*
	 * 
	 * Due to our EditDocument implementation, we cannot produce a test for this class/command.
	 * Our EditDocument class just lets the user edit the textArea of the window app and write whatever
	 * he wants.
	 * 
	 */
	@Test
	void test() {
		fail("Cannot implement this.");
	}
}
// The ActionListener inteface, which is implemented by the commands' classes.

package commands;

import java.awt.event.*;

public interface ActionListener{
	
	public void actionPerformed(ActionEvent e);
}